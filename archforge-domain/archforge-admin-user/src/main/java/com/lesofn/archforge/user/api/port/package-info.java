/**
 * 领域端口包：admin-user 对外声明的外部能力契约（依赖倒置的 domain 侧），
 * 由 infrastructure 实现或 server 层装配。所属模块为 OPEN，无需 NamedInterface。
 */
@NullMarked
package com.lesofn.archforge.user.api.port;

import org.jspecify.annotations.NullMarked;
